# Manuscript

Compile `main.tex` with an ACM-compatible LaTeX environment. The repository includes the class and bibliography style used for the current draft for reproducibility; verify the final EDBT author instructions before submission.

The architecture diagram is embedded as TikZ. Operational-state colors are deliberately distinct from workflow-stage colors.

The PDF is a preview of the current manuscript and is not evidence of acceptance or publication.
